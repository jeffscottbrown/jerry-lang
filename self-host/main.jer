// Stdin driver: lex, parse, and print the AST of Jerry source.
// Usage: jerry run self-host/ < source.jer
fn main() {
    let src: string = read_stdin();
    let prog: Program = parse(src);
    print(prog.to_string());
}
